<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Card.Large">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@android:id/icon"
            style="@style/CardIcon.Droplet"
            android:duplicateParentState="true"
            android:importantForAccessibility="yes"
            android:contentDescription="@string/accessibility_icon_root"
            android:transitionName="icon_root"
            android:src="@drawable/ic_root_24" />

        <TextView
            android:id="@android:id/title"
            style="@style/CardTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/m3e_spacing_large"
            android:layout_weight="1"
            android:text="@string/home_root_title" />

    </LinearLayout>

    <TextView
        android:id="@android:id/text1"
        style="@style/CardSummary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="@string/home_root_description" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <com.google.android.material.button.MaterialButton
            android:id="@android:id/button1"
            style="@style/CardButton"
            android:enabled="false"
            android:text="@string/home_root_button_start"
            android:contentDescription="@string/accessibility_button_start"
            app:icon="@drawable/ic_server_start_24" />

        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/lottie_start"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center_vertical|start"
            android:layout_marginStart="12dp"
            android:visibility="gone"
            app:lottie_autoPlay="false"
            app:lottie_loop="true" />

        <com.google.android.material.button.MaterialButton
            android:id="@android:id/button2"
            style="@style/CardButton"
            android:enabled="false"
            android:text="@string/home_root_button_restart"
            android:visibility="gone"
            android:contentDescription="@string/accessibility_button_restart"
            app:icon="@drawable/ic_server_restart"
            tools:visibility="visible" />

    </FrameLayout>

</LinearLayout>