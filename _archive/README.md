# _archive — retired scratch files

One-off scripts, logs, and notes that used to live in the repo root. Archived
2026-07-12 instead of deleted so past AI/dev sessions can be reconstructed.
Nothing here is wired into the build or CI — do not "fix" or re-run anything
from this directory.

| Dir | Contents |
|-----|----------|
| `issue-automation/` | One-off GH issue/comment scripts (May–Jun 2026 issue triage waves) |
| `logs/` | Captured CI/build failure logs and secret-scanning output |
| `notes/` | Release-note drafts, issue dumps (txt/json), PR descriptions, retro notes |
| `patches/` | Saved diffs and pre/post refactor snapshots (`new_/old_AdbKey.kt`) |
| `misc/` | One-off fix/refactor scripts, test scraps, artifact-install helpers |
